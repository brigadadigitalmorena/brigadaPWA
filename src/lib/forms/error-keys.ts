/** Canonical constraint / validation error keys for Form Engine. */
export const ERROR_KEYS = {
  CONSTRAINT_COMPARISON_FAILED: 'CONSTRAINT_COMPARISON_FAILED',
  REQUIRED_FIELD: 'REQUIRED_FIELD',
  INVALID_VALUE: 'INVALID_VALUE',
} as const;
